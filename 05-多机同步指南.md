# 05 · 双笔记本同步指南

> 两台机器共用同一个 GitHub 仓库，不需要网盘、不需要 U 盘拷贝。
> **GitHub 是唯一的真相源**，两台笔记本各持有一份本地副本，靠 `pull` / `push` 对齐。

---

## 一、先回答：今天的进展怎么共享

**已经共享了。** 今天（D001）的所有改动我都已推送到
<https://github.com/owen0278/fde-learning-plan>。

第二台笔记本配置完成后执行 `git clone`，拿到的就是包含今日进展的最新版本。
以后每天收工前 `git push`，另一台开工前 `git pull`，进度就自动对齐了。

---

## 二、两条不能破的规矩

### 规矩 1：两台机器用同一个 git 身份

```
user.name  = owen0278
user.email = 027788@163.com
```

**为什么这是硬要求**：GitHub 的绿墙是按提交邮箱匹配账号的。
两台机器身份不一致的话，一半的提交不会被算进你的贡献图 —— 绿墙会变成花的。

> 注意：第二台笔记本上 WorkBuddy 登录的是「晨曦」账号，
> 那是 WorkBuddy 的身份，**跟 git 身份是两回事**，互不影响。
> git 身份照样填上面这一组。

### 规矩 2：每台机器各自生成 SSH 密钥

**不要把第一台的私钥复制到第二台。** 私钥一旦外泄就得重新生成、重新授权，很麻烦。

GitHub 一个账号可以挂**多把公钥**，两台机器各配一把是标准做法，
以后某台电脑丢了，只删那一把就行。

---

## 三、第二台笔记本的完整配置

> 全程在 **PowerShell** 里执行。装完 Git 后要先**关掉 PowerShell 重开一次**。

### Step 1 · 安装 Git

第二台大概率也没装独立 Git。winget 会卡（Git 安装包托管在 GitHub，国内慢），
**直接走国内镜像**：

1. 打开 <https://registry.npmmirror.com/-/binary/git-for-windows/v2.55.0.windows.3/>
2. 下载 `Git-2.55.0.3-64-bit.exe`（约 63MB）
3. 双击安装，一路"下一步"，默认选项即可

装完重开 PowerShell，验证：

```powershell
git --version
```

应输出 `git version 2.55.0.windows.3`。

### Step 2 · 配置 git 身份（关键）

```powershell
git config --global user.name "owen0278"
git config --global user.email "027788@163.com"
git config --global core.editor "code --wait"
git config --global core.autocrlf true
git config --global pull.rebase true
```

最后一行 `pull.rebase true` 很重要 —— 它让 `git pull` 默认用 rebase 而不是 merge，
能避免产生一堆无意义的 "Merge branch" 提交，两台机器同步时尤其明显。

验证：

```powershell
git config --global --get user.email
```

必须是 `027788@163.com`。

### Step 3 · 生成 SSH 密钥并添加到 GitHub

```powershell
ssh-keygen -t ed25519 -C "027788@163.com"
```

一路回车（不要设密码短语）。然后取出公钥：

```powershell
cat ~/.ssh/id_ed25519.pub
```

把输出的那一整行（`ssh-ed25519 AAAA... 027788@163.com`）复制出来，到
**<https://github.com/settings/ssh/new>**：

- Title 填 **笔记本B**（跟第一台的「工作电脑」区分开）
- 粘贴公钥 → Add SSH key

验证：

```powershell
ssh -T git@github.com
```

输入 `yes`，看到 `Hi owen0278!` 就成了。

### Step 4 · 克隆仓库

**路径必须用英文。** 推荐放 `C:\fde-plan` —— 短、无中文、无空格，
而且不依赖用户名（两台机器用户名可能不同）。

```powershell
cd C:\
git clone git@github.com:owen0278/fde-learning-plan.git fde-plan
cd fde-plan
ls
```

应该看到 `README.md`、`weeks\`、`day01\`、`04-GitHub推送指南.md` 等。

### Step 5 · 安装 Python 3.12 + uv

```powershell
winget install Python.Python.3.12
winget install astral-sh.uv
```

装完**重开 PowerShell**，验证：

```powershell
python --version
uv --version
```

> 如果 `python` 提示找不到或弹出微软商店：
> Windows 设置 → 应用 → 高级应用设置 → 应用执行别名 →
> 把 `python.exe` / `python3.exe` 两个开关**关掉**。
> 详细排查见 `weeks\W01-Python现代工程化\D001-环境安装操作手册.md`。

### Step 6 · 重建练习环境

`.venv` 被 `.gitignore` 排除了，不会同步（这是对的），所以要在每台机器上单独重建：

```powershell
cd C:\fde-plan\day01\hello-fde
uv sync
uv run hello-fde
```

应输出 `Hello from hello-fde!`。

以后每新建一个练习目录，都要在各自机器上跑一次 `uv sync`。

### Step 7 · 安装 VS Code + 扩展

```powershell
winget install Microsoft.VisualStudioCode
```

装完重开终端，然后：

```powershell
code --install-extension ms-python.python
code --install-extension ms-python.vscode-pylance
code --install-extension charliermarsh.ruff
code --install-extension eamodio.gitlens
```

---

## 四、每天的固定动作（双机工作流）

### 开工前 · 必做

```powershell
cd C:\fde-plan
git pull --rebase
```

**这一步不能省。** 跳过它就是两台机器冲突的唯一原因。

### 收工后 · 必做

```powershell
cd C:\fde-plan
git add .
git commit -m "docs: 完成 D00X + 一句话说明"
git push
```

`commit` 每天做，`push` 至少每天做一次 —— 绿墙按提交日期算，不是推送日期。

### 一天内换机器怎么办

比如上午在笔记本 A 学完推上去了，晚上用笔记本 B：

```powershell
cd C:\fde-plan
git pull --rebase
```

拉到最新再动手，收工照样 commit + push。

---

## 五、冲突处理

### 症状：push 被拒

```
! [rejected] main -> main (fetch first)
```

**原因**：另一台机器推送了你本地没有的提交。

**解决**（不要慌，不会丢东西）：

```powershell
git pull --rebase
git push
```

### 症状：pull 时提示 CONFLICT

说明两台机器改了**同一个文件的同一段**（最常见是 `02-进度追踪.md`）。

```powershell
git status
```

会列出冲突文件。打开它，会看到：

```
<<<<<<< HEAD
（你这台的內容）
=======
（另一台的內容）
>>>>>>> origin/main
```

手动改成你想要的样子，删掉那三行标记，然后：

```powershell
git add .
git rebase --continue
git push
```

### 预防冲突的最好办法

**`02-进度追踪.md` 这类两份都可能改的文件，约定只在笔记本 A（主工作机）更新。**
第二台只改代码和当天任务卡的复盘部分（每个文件只有当天会动，天然不冲突）。

---

## 六、第二台的每日提醒（可选）

笔记本 A 上有两个定时自动化（每天 08:30 推任务、每周日 20:00 门槛自检）。

如果第二台也想收到，需要在那台机器的 WorkBuddy 会话里重新建一次，
**注意路径要改成 `C:\fde-plan\weeks\`**。让那边的助手照抄这两个自动化的配置即可。

---

## 七、验收清单

第二台配置完成后逐项核对：

- [ ] PowerShell 里 `git --version` 有输出
- [ ] `git config --global --get user.email` 返回 `027788@163.com`
- [ ] `ssh -T git@github.com` 返回 `Hi owen0278!`
- [ ] GitHub SSH keys 页面能看到两把公钥（工作电脑 + 笔记本B）
- [ ] `C:\fde-plan` 目录存在，`ls` 能看到 weeks\ 和 day01\
- [ ] `uv run hello-fde` 输出 `Hello from hello-fde!`
- [ ] `code --version` 有输出，四个扩展已装
- [ ] `git pull --rebase` 能正常跑通

---

## 八、几个常见疑问

**Q：两台机器能同时改文件吗？**
不建议。同一时刻只在一台上动同一个文件，改完 push，另一台 pull。

**Q：`.env` 文件会同步吗？**
不会，`.gitignore` 里排除了。**这是故意的** —— API Key 不能进公开仓库。
第二台需要自己建 `.env`，填自己的 key。

**Q：为什么第二台 clone 下来跑不了 Python？**
因为 `.venv` 不同步。进到练习目录跑一次 `uv sync` 就好。

**Q：能不能用网盘同步？**
不要。网盘同步 git 仓库会导致 `.git` 目录损坏，这是经典事故。
就用 pull / push。

**Q：大文件、截图放哪？**
小于 50MB 的学习资料直接放仓库里。
特别大的文件（模型权重、数据集）不要进 git，用 WorkBuddy 的项目共享文件（Project Drive）单独上传。
